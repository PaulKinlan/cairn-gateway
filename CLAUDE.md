Read and follow `AGENTS.md`, then `PLAN.md`. `PLAN.md` is the product and milestone source of truth.
Do not revive fixture-only Stage 0 scope or defer planned UI/OAuth/provider/deployment work when its
active milestone gates are satisfied. Do not claim progress from regression counts, status pages,
health endpoints, evidence prose, or security work without the milestone's user journey.

Retain Cairn's permanent boundary: never print/return provider credentials or signing material, and
never add generic proxying, arbitrary destinations, token export, or credential reveal. Obey
milestone gates and explicit no-push/no-deploy instructions.
