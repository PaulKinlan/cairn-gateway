# M1 acceptance — usable local product

- **Milestone:** M1 usable local product
- **Accepted implementation source:** `90808f8030beed074e0fbe04f130c6bb0f5152f5`
- **Accepted product/evidence state:** candidate commit containing this record; its exact review
  decision is attached with Git note ref `refs/notes/acceptance`.
- **Owner/admin actions:** create/reset fixture context; record agent/device/workload display
  labels; create, inspect, revoke, deny-test, and replace a versioned five-call 24-hour grant;
  inspect bounded receipt, usage, and audit views.
- **Agent/workload actions:** initialize/reconnect Streamable HTTP; search, describe, inspect
  connection status, invoke the fixed fixture operation, observe revocation denial, and invoke after
  replacement.
- **Local URL:** `http://127.0.0.1:8787/` while `deno task local:run` is active.
- **Demo/runbook:** `deno task local:demo`, `docs/local-setup.md`,
  `docs/evidence/m1/browser-validation.md`.
- **Implementation/security review:** parallel run `d8827b97-2ee0-4d9d-9f7d-5cf5da37193e`
  - Product/lifecycle reviewer: **ACCEPT — M1 partial submilestone**, no blocker.
  - Security reviewer: **ACCEPT**, no blocker.
- **Evidence-record review:** run `f9db229d-1417-472c-a3e0-244e503e458f`, **ACCEPT** after the sole
  stale-runbook contradiction was corrected in `4b0954aab3afcf1c7d44b291846a812618c30228`.
- **Exact-source browser validation:** Chrome DevTools MCP ran the visible create → label → grant →
  allow → revoke → deny → replace → allow journey and a two-session MCP reconnect against
  `0e5a584bda425ce37271354be664de1826f9faca`. The committed evidence records browser, route,
  interactions, rendering, Lighthouse scores, screenshots, and hashes.
- **Residual boundary:** M1 maps honest display labels to a fixed closed cryptographic fixture
  authority. Genuine owner, agent, device, and workload enrollment is M3. Named-client compatibility
  is M5. Durable state, provider OAuth/custody, hosted authority, and production use are not
  claimed.
