# Cairn Gateway agent rules

Private/internal, offline-first security prototype. Never access credentials, create provider or
hosting resources, add generic request/token/credential surfaces, deploy, push, or create a remote.
Unknown vendor behavior blocks live activation. Run `deno task check` before a local commit.
